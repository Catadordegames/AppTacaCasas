# Changelog 2026-04-21

---

- 🏷️ **Status:** [✅] Concluído
- 📅 **Data:** 2026-04-21
- 📝 **Atividade:** Auditar e Corrigir Pipeline Deploy YAML
- 🎯 **Objetivo:** Auditar `.github/workflows/deploy.yml` para assegurar que as definições de setup inicial e migrações alcancem produção após a refatoração MVC.
- 📂 **Locais:** `backend/package.json`, `.github/workflows/deploy.yml`
- 🛠️ **Implementacoes:**
  - Correção dos scripts cruciais no escopo de `package.json` (`main`, `start`, `dev`) que antes apontavam para o antigo diretório raiz (exvazio) redirecionando limpos pro subdiretório `src/`.
  - Troca da ordem do PM2 no Action de Deploy para se acoplar estritamente à topologia e nomes orquestrados pelo recém criado `Ecosystem.config.js`.
- ✅ **Resultado:** Os deploys automáticos agora religam o app corretamente sob o novo esqueleto, o que por consequência ativará a rotina de migrations autônoma.

---

- 🏷️ **Status:** [✅] Concluído
- 📅 **Data:** 2026-04-21
- 📝 **Atividade:** Restauração do Repositório de Auth (`AuthRepository.buscarPorNome is not a function`)
- 🎯 **Objetivo:** Corrigir a tela de Login quebrando no backend após o agente anterior ter sobrescrito erroneamente um arquivo de regras com um de controller.
- 📂 **Locais:** `backend/src/repositories/auth.repository.js`
- 🛠️ **Implementacoes:**
  - Recriação literal do objeto `AuthRepository` contendo a query SQL `SELECT * FROM professores WHERE nome = ?`.
  - Re-exportação limpa do método `buscarPorNome` para garantir a ponte perfeita com o `AuthService.login()`.
- ✅ **Resultado:** Rota de Login volta a ler o banco de dados dinamicamente testando os hashes.

---

- 🏷️ **Status:** [✅] Concluído
- 📅 **Data:** 2026-04-21
- 📝 **Atividade:** Injeção de Usuários Padrão via Migration
- 🎯 **Objetivo:** Adicionar os usuários teste `admin` e `professor` ao banco usando migrations, ambos com senha "Liberdade" apropriadamente criptografada em hash bcrypt.
- 📂 **Locais:** `backend/migrations/002_insert_default_users.sql`
- 🛠️ **Implementacoes:**
  - Geração segura de bcrypt hash com 10 rounds usando o node.js contêinerizado para a senha `Liberdade`.
  - Criação da migration indexada `002` com os schemas de Insert em `professores`.
  - Uso das permissões corretas (Admin = 1, Professor = 2) obedecendo à refatoração padronizada existente.
- ✅ **Resultado:** Usuários garantidos imediatamente em qualquer restart ou novo start local via o próprio loop de migrations.

---
