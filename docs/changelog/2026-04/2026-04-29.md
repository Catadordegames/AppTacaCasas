# Changelog 2026-04-29

---

- 🏷️ **Status:** [✅] Concluído
- 📅 **Data:** 2026-04-29
- 📝 **Atividade:** Listagem completa de lançamentos com edição e exclusão
- 🎯 **Objetivo:** Exibir todos os lançamentos para todos os usuários autenticados e permitir edição/exclusão com controle de permissão (próprio lançamento ou admin).
- 📂 **Locais:** `backend/src/services/lancamentos.service.js`, `backend/src/repositories/lancamentos.repository.js`, `backend/src/controllers/lancamentos.controller.js`, `backend/src/routes/lancamentos.routes.js`, `frontend/src/hooks/useListagemLancamentos.js`, `frontend/src/views/professor/ListagemLancamentos.jsx`
- 🛠️ **Implementações:**
  - Backend: removido filtro forçado de `professor_id` no `service.listar` — todos os usuários autenticados agora veem todos os lançamentos.
  - Backend: adicionado método `atualizar` no service, repository e controller (pontuação e justificativa_snapshot editáveis).
  - Backend: adicionada rota `PUT /lancamentos/:id` com autenticação e controle de permissão.
  - Frontend: hook `useListagemLancamentos` reescrito com suporte a edição e exclusão, expondo `podeEditar`, `isAdmin`, estado do modal e handlers.
  - Frontend: view `ListagemLancamentos.jsx` atualizada com botões Editar (lápis) e Excluir (lixeira) em cada card, visíveis apenas para quem tem permissão.
  - Frontend: modal de edição com campos de justificativa e pontuação, com feedback visual de bônus/penalidade.
- ✅ **Resultado:** Todos os usuários veem todos os lançamentos. Edição e exclusão disponíveis para o autor do lançamento e para admins.

---

- 🏷️ **Status:** [✅] Concluído
- 📅 **Data:** 2026-04-29
- 📝 **Atividade:** Correção de bug na edição de professores (casa e perfil de permissão)
- 🎯 **Objetivo:** Garantir que ao editar um professor, os campos `casa_id` e `permissao` sejam corretamente enviados ao backend, refletindo as alterações feitas pelo administrador.
- 📂 **Locais:** `frontend/src/hooks/useAdminProfessores.js`, `frontend/src/views/admin/AdminProfessores.jsx`
- 🛠️ **Implementações:**
  - Identificado bug raiz: os labels das `<option>` do select de perfil estavam invertidos em relação à convenção do banco (`ADMIN = 1`, `PROFESSOR = 2`).
  - `<option value="1">` agora exibe "Coordenação / Admin" e `<option value="2">` exibe "Professor".
  - Corrigida a renderização da coluna na tabela: `v === 1` exibe badge "ADMIN", caso contrário "Professor".
  - Corrigida a conversão String() em `abrirEditar` para garantir consistência de tipos nos selects controlados do React.
  - Removido log de debug temporário adicionado durante diagnóstico.
- ✅ **Resultado:** Os selects de casa e perfil de permissão agora refletem e enviam corretamente os valores editados pelo administrador.
